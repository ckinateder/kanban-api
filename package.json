{
  "name": "kanban-server",
  "version": "1.0.0",
  "description": "API for Kanban Board",
  "main": "api.js",
  "scripts": {    
    "start": "node api.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Calvin Kinateder",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.12.1"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  },
  "engines": {
    "node": "14.x"
  }
}
